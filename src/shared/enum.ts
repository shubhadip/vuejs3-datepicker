export enum ValidationMessages {
  required = 'Please enter this value',
}

export enum VALIDATORS {
  REQUIRED = 'required',
}

export enum KeyName {
  Enter = 'Enter',
  ArrowUp = 'ArrowUp',
  ArrowDown = 'ArrowDown',
  Escape = 'Escape',
  Tab = 'Tab',
}
